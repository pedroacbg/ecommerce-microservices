server:
  port: 8082
spring:
  application:
    name: user
  datasource:
    url: jdbc:postgresql://localhost:5432/ecom_user
    username: ecommerce
    password: ecommerce
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect